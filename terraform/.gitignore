# Terraform local state and cache
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup

# Lambda build artifacts (regenerated by archive_file on every apply)
*.zip

# Variable files that might contain secrets (none yet, but good habit)
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log